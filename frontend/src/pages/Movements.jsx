import{useEffect,useState}from'react';import Sidebar from'../components/Sidebar';import Navbar from'../components/Navbar';import{getMovements}from'../services/api';export default function Movements(){const[r,setR]=useState([]),[q,setQ]=useState(''),[t,setT]=useState('All Types');useEffect(()=>{getMovements().then(setR)},[]);const x=r.filter(a=>(a.product+a.sku+a.reason+a.username).toLowerCase().includes(q.toLowerCase())&&(t==='All Types'||a.movement_type===t));return <Shell><div className='page-header'><div><h2>Movement History</h2><p>Track every inventory movement</p></div></div><div className='panel'><div className='toolbar'><input className='search-input' value={q} onChange={e=>setQ(e.target.value)} placeholder='Search movements...'/><select value={t} onChange={e=>setT(e.target.value)}><option>All Types</option><option>IN</option><option>OUT</option></select></div><Table rows={x.map(a=>[new Date(a.created_at).toLocaleString(),a.product,a.warehouse,<span className={'badge '+(a.movement_type==='IN'?'success':'danger')}>{a.movement_type}</span>,(a.movement_type==='IN'?'+':'-')+a.quantity,a.reason,a.username])}/></div></Shell>};const Shell=({children})=><div className='app-layout'><Sidebar/><div className='main-area'><Navbar/><main className='page-content'>{children}</main></div></div>;const Table=({rows})=><div className='table-wrapper'><table><thead><tr>{['Date','Product','Warehouse','Type','Quantity','Reason','User'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((x,j)=><td key={j}>{x}</td>)}</tr>)}</tbody></table></div>;
