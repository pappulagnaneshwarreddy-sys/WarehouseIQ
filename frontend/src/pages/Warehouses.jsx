import{useEffect,useState}from'react';import Sidebar from'../components/Sidebar';import Navbar from'../components/Navbar';import{getWarehouses,createWarehouse}from'../services/api';const Shell=({children})=><div className='app-layout'><Sidebar/><div className='main-area'><Navbar/><main className='page-content'>{children}</main></div></div>;export default function Warehouses(){const[r,setR]=useState([]),[o,setO]=useState(false),[f,setF]=useState({name:'',location:'',bin_code:'',capacity:''});const load=()=>getWarehouses().then(setR);useEffect(load,[]);async function save(e){e.preventDefault();await createWarehouse(f);setO(false);setF({name:'',location:'',bin_code:'',capacity:''});load()}return <Shell><div className='page-header'><div><h2>Warehouses</h2><p>Manage warehouse locations, bins and capacity</p></div><button className='primary-button' onClick={()=>setO(true)}>+ Add Warehouse</button></div><div className='panel'><Table rows={r.map(x=>[x.name,x.location,x.bin_code,Number(x.capacity).toLocaleString()+' units',x.current_stock,(x.capacity?Math.round(x.current_stock/x.capacity*100):0)+'%'])}/></div>{o&&<div className='modal-backdrop'><div className='modal'><div className='modal-header'><h3>Add Warehouse</h3><button onClick={()=>setO(false)}>×</button></div><form className='form-panel modal-form' onSubmit={save}>{[['Name','name'],['Location','location'],['Bin','bin_code'],['Capacity','capacity']].map(([l,k])=><label key={k}>{l}<input required type={k==='capacity'?'number':'text'} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}<div className='form-actions'><button type='button' className='secondary-button' onClick={()=>setO(false)}>Cancel</button><button className='primary-button'>Save Warehouse</button></div></form></div></div>}</Shell>};const Table=({rows})=><div className='table-wrapper'><table><thead><tr>{['Warehouse','Location','Bin','Capacity','Current Stock','Utilization'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((x,j)=><td key={j}>{x}</td>)}</tr>)}</tbody></table></div>;
