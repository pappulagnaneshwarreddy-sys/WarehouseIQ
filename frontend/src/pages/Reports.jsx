import{useEffect,useState}from'react';import Sidebar from'../components/Sidebar';import Navbar from'../components/Navbar';import{getReportSummary}from'../services/api';export default function Reports(){const[d,setD]=useState();useEffect(()=>{getReportSummary().then(setD)},[]);return <Shell><div className='page-header'><div><h2>Reports</h2><p>Live reports generated from the database</p></div></div>{d&&<><div className='report-grid'><Card title='Inventory Report'><p>{d.inventory.length} products tracked.</p><button className='primary-button' onClick={()=>window.print()}>Print Report</button></Card><Card title='Movement Report'><p>{d.movement_totals.reduce((a,x)=>a+x.movement_count,0)} movements recorded.</p><button className='primary-button' onClick={()=>window.print()}>Print Report</button></Card><Card title='Supplier / Warehouse'><p>{d.supplier_count} suppliers and {d.warehouse_count} warehouses.</p><button className='primary-button' onClick={()=>window.print()}>Print Report</button></Card></div><div className='panel'><div className='panel-header'><h3>Inventory Report</h3></div><Table rows={d.inventory.map(x=>[x.name,x.sku,x.total_stock,x.reserved_stock,x.available_stock,'₹'+Math.round(x.stock_value).toLocaleString('en-IN')])}/></div></>}</Shell>};const Card=({title,children})=><div className='report-card'><h3>{title}</h3>{children}</div>;const Shell=({children})=><div className='app-layout'><Sidebar/><div className='main-area'><Navbar/><main className='page-content'>{children}</main></div></div>;const Table=({rows})=><div className='table-wrapper'><table><thead><tr>{['Product','SKU','Total','Reserved','Available','Value'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((x,j)=><td key={j}>{x}</td>)}</tr>)}</tbody></table></div>;
