import{useEffect,useState}from'react';import Sidebar from'../components/Sidebar';import Navbar from'../components/Navbar';import{getProducts,getWarehouses,stockIn,stockOut}from'../services/api';export function StockForm({type}){const[p,setP]=useState([]),[w,setW]=useState([]),[f,setF]=useState({product_id:'',warehouse_id:'',quantity:'',reason:''}),[msg,setMsg]=useState(''),[err,setErr]=useState('');useEffect(()=>{Promise.all([getProducts(),getWarehouses()]).then(([a,b])=>{setP(a);setW(b);setF(x=>({...x,product_id:a[0]?.id||'',warehouse_id:b[0]?.id||''}))})},[]);async function submit(e){e.preventDefault();setMsg('');setErr('');try{const u=JSON.parse(localStorage.getItem('warehouseiq_user')||'{}');await(type==='IN'?stockIn:stockOut)({...f,username:u.username||'admin'});setMsg(`Stock ${type} recorded successfully.`);setF(x=>({...x,quantity:'',reason:''}))}catch(x){setErr(x.message)}}return <Shell><div className='page-header'><div><h2>Stock {type}</h2><p>Record {type==='IN'?'incoming':'outgoing'} inventory</p></div></div><form className='form-panel' onSubmit={submit}><label>Product<select required value={f.product_id} onChange={e=>setF({...f,product_id:e.target.value})}>{p.map(x=><option value={x.id} key={x.id}>{x.name} ({x.sku})</option>)}</select></label><label>Warehouse<select required value={f.warehouse_id} onChange={e=>setF({...f,warehouse_id:e.target.value})}>{w.map(x=><option value={x.id} key={x.id}>{x.name} — {x.location}</option>)}</select></label><label>Quantity<input required min='1' type='number' value={f.quantity} onChange={e=>setF({...f,quantity:e.target.value})}/></label><label>Reason<input required value={f.reason} onChange={e=>setF({...f,reason:e.target.value})} placeholder={type==='IN'?'Purchase, return, received from supplier...':'Dispatch, sale, damaged, etc.'}/></label>{msg&&<p className='form-success'>{msg}</p>}{err&&<p className='form-error'>{err}</p>}<div className='form-actions'><button type='button' className='secondary-button' onClick={()=>setF({...f,quantity:'',reason:''})}>Clear</button><button className='primary-button'>Stock {type}</button></div></form></Shell>};const Shell=({children})=><div className='app-layout'><Sidebar/><div className='main-area'><Navbar/><main className='page-content'>{children}</main></div></div>;export default function StockIn(){return <StockForm type='IN'/>}
